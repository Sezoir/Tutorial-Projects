#include <stdio.h>

void main()
{
	char s[] = "string";	// Or you can use: { 's', 't', 'r', 'i', 'n', 'g', 0 }
	// A string is like a array of characters
	printf("String is: %s\n", s);

	while (int a = 1); {} //Keeps console up
}