#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>

void main()
{
	const char * fn1 = "file1";
	const char * fn2 = "file2";

	/*
	FILE * fh = fopen(fn1, "w"); // Creates the file.
	fclose(fh);
	puts("File Created"); 
	
	rename(fn1, fn2);	// Renames the file in the first argument with the string in the second argument.
	
	remove(fn2);	// Deletes the filename.
	puts("File Deleted"); 
	*/

	while (true) {};
}