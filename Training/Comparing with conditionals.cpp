#include <stdio.h>

void main()
{
	int a = 1; // With intergers, 1 is associated with true and 0 with false.
	int c = 1; // This is because is C there is no boolein type, however in C++ there is

	int c = a ? 7 : 9; // If the condition is true then it will print the first value, if not then the second value.

	printf("c is %d\n", c);





	/*if (a)
	{
	puts("This is true");
	}
	else if (b)
	{
	puts("This is not true");
	}
	else
	{
	puts("Nothing is true");
	} */

	while (int a = 1); {} //Keeps console up
}